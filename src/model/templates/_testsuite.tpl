<testsuite disabled="{{data.disabled}}" errors="{{data.errors}}" failures="{{data.failures}}" hostname="{{data.hostname}}" id="{{data.id}}" name="{{data.name}}" package="{{data.package}}" skipped="{{data.skipped}}" tests="{{data.tests}}" time="{{data.time}}" timestamp="{{data.timestamp}}" >{{data.body}}</testsuite>